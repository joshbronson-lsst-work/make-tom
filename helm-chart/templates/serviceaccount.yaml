{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "tom-deploy.serviceAccountName" . }}
  labels:
{{ include "tom-deploy.labels" . | indent 4 }}
{{- end -}}
